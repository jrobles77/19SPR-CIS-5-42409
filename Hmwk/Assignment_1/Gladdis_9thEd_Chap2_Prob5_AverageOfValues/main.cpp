#include <iostream>
using namespace std;

int main()
{
    double sum, average, one = 28, two = 32, three = 37, four = 24, five = 33;
    sum = one + two + three + four + five;
    average = sum / 5;
            cout << "The average of the variables is " << average;
    return 0;
}